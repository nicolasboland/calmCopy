export interface IProps {
  image: string,
  title: string,
  text: string,
  carousel?: boolean,
  is30Noches?: boolean,
  isPickUp?: boolean,
  isHotSale?: boolean,
  altImage: string,
  index: number,
}
