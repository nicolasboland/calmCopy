export interface IProps {
    title: string
    original: {
        arrCapas: string[]
        specs: {
            text: string
            icon: string
        }[]
    }
    hibrido: {
        arrCapas: string[]
        specs: {
            text: string
            icon: string
        }[]
    }
}